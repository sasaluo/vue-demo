/**
 * Created by ddy on 2017/2/17.
 */
export const login = r => {
    require(['./login.vue'], r)
};

export const register = r => {
    require(['./register.vue'], r)
};

export const setPassword = r => {
    require(['./setPassword.vue'], r)
};

export const setCode = r => {
    require(['./setCode.vue'], r)
};